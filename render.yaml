services:
  - type: web
    name: cmdmate-server
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn server.cmdmate_server:app --host 0.0.0.0 --port 10000
    plan: free